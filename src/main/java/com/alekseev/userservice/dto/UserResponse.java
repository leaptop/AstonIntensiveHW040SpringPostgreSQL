package com.alekseev.userservice.dto;

/**
 * DTO для ответов (только чтение).
 * Record → автоматически: constructor, getters (name(), email() и т.д.), equals/hashCode/toString.
 *
 * UserResponse — это DTO (Data Transfer Object) для ответов от API клиенту (например, фронтенду,
 * Postman или другому сервису). Это не ответ от БД, а специально подготовленный объект для передачи
 * по сети (в JSON-формате).
 *
 * Кто кому отвечает:
 * БД (через репозиторий) возвращает Entity (User) — полный объект с внутренними полями (включая createdAt,
 * который не нужен клиенту).
 * Сервис берёт Entity, маппит (копирует) нужные поля в UserResponse (без createdAt, чтобы не раскрывать
 * детали) и возвращает его контроллеру.
 * Контроллер сериализует UserResponse в JSON и отправляет клиенту (тому, кто сделал HTTP-запрос,
 * например, браузер или приложение).
 *
 * Почему DTO, а не Entity напрямую: Entity — для БД (с аннотациями JPA), DTO — для API (лёгкий,
 * безопасный, с валидацией). Это разделение слоёв: БД не знает о клиенте, клиент не знает о БД.
 * Record (тип) делает его immutable (нельзя изменить после создания), что безопасно.
 */
public record UserResponse(
        Long id,
        String name,
        String email,
        Integer age
) {
}/*Вопрос: Ты пишешь: "DTO для входящих данных". Откуда и куда данные являются входящими? Всегда объясняй подобное.
Если здесь есть проверки типа проверки электронной почты, то предположу, что данные входят от человека, который
мог ошибиться. Потому, что у UserResponse например нет таких проверок. Почему?
Ответ:
UserRequest.javaDTO (Data Transfer
Object) — это специальный класс (или, в данном случае, record в Java), предназначенный исключительно для передачи
данных между разными частями системы. Он не содержит бизнес-логики (как вычисления или сложные операции), а только
поля с данными и, иногда, простые преобразования (как trim() здесь). DTO помогает разделить слои приложения: например,
данные из внешнего мира (входящие) адаптируются к внутренним моделям (Entity для БД), и наоборот. Это предотвращает
утечку деталей (например, не отдаём клиенту поля из БД, которые ему не нужны), упрощает сериализацию в JSON и повышает
безопасность.
"DTO для входящих данных": Откуда и куда данные?

Откуда входящие: Данные приходят от внешнего клиента (например, от пользователя через веб-браузер, мобильное приложение,
 Postman или другой сервис). Конкретно: это тело HTTP-запроса (JSON), которое отправляется на эндпоинт API
 (в контроллере, как @PostMapping или @PutMapping). Spring автоматически парсит JSON в объект UserRequest с помощью
 Jackson (библиотека для JSON в Spring Web). Например, клиент отправляет POST /api/users с телом {"name": "Stepan",
 "email": "stepan@example.com", "age": 30} — это десериализуется в UserRequest.
Куда входящие: Из контроллера данные передаются в сервис-слой (UserService), где маппятся в Entity (User) для
сохранения в БД. Таким образом, UserRequest — "мост" от внешнего мира (незащищённого, с возможными ошибками) в
внутреннюю логику приложения. После обработки сервис возвращает ответ (UserResponse), который контроллер отправляет
обратно клиенту.

Да, твоё предположение верно: данные "входящие" от человека (или внешней системы), который мог ошибиться (неправильный
email, пустое имя, отрицательный возраст). Поэтому здесь валидация (@NotBlank, @Email, @Size, @Min) — это аннотации из
Jakarta Bean Validation (библиотека, включённая в spring-boot-starter-validation). Они автоматически проверяют данные
перед входом в метод контроллера (благодаря @Valid). Если данные неверные — Spring кинет исключение (400 Bad Request)
с сообщением об ошибке, не давая им пройти дальше. Под капотом: Hibernate Validator (реализация Bean Validation)
сканирует поля и применяет правила (например, @Email проверяет формат по regex как user@domain.com).
Почему в UserResponse нет таких проверок?
UserResponse — это DTO для исходящих данных (ответов от сервера клиенту), т.е. данные, которые приложение само
генерирует после обработки.

Нет валидации, потому что сервер доверяет своим данным: они уже проверены на входе (в UserRequest), обработаны в
сервисе и взяты из БД (Entity). Нет риска ошибок ввода — сервер не позволит сохранить неверные данные.
UserResponse проще: он только для чтения (record делает его immutable — неизменяемым после создания), без конструкторов
для модификации. Его цель — безопасно отдать клиенту минимум информации (без createdAt из Entity, чтобы не раскрывать
внутренние детали БД).
Если бы валидация была нужна (редко), её добавили бы, но для исходящих DTO это избыточно — фокус на производительности
и простоте сериализации в JSON.

Compact constructor в UserRequest
Это фича Java records (с Java 14+): специальный конструктор без параметров, где можно модифицировать компоненты (поля)
перед их финализацией. Здесь он trim() имя и email (убирает пробелы по краям), чтобы очистить данные от лишнего
(например, если клиент отправил " Stepan "). Под капотом: Компилятор Java генерирует канонический конструктор
(с параметрами name, email, age), но compact позволяет "перехватить" присвоение. Это заменяет ручной trim в
сервисе/контроллере.
В целом, такой дизайн следует принципу разделения ответственности: входящие DTO с валидацией для защиты от "грязных"
данных, исходящие — чистые и минималистичные. Если код в другом контексте (не Spring) — валидацию пришлось бы делать
вручную.

 */
